import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  container: {
    position: "relative",
    maxHeight: "80vh",
    overflow: "hidden",
  },
  circularProgress: {
    margin: "0 auto",
  },
});
